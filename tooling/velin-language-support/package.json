{
  "name": "velin-language-support",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "lsp-server",
    "vscode-extension",
    "shared"
  ],
  "scripts": {
    "build": "npm run build -w lsp-server && npm run build -w vscode-extension",
    "clean": "npm run clean -w lsp-server && npm run clean -w vscode-extension",
    "dev:lsp": "npm run dev -w lsp-server",
    "dev:vscode": "npm run dev -w vscode-extension",
    "package:vscode": "npm run package -w vscode-extension",
    "publish:vscode": "npm run publish -w vscode-extension",
    "test": "echo 'Running workspace build test...' && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "@types/node": "^18.0.0"
  }
}